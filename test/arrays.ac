fn sum3(a: i32, b: i32, c: i32) -> i32 {
    return a + b + c
}

fn main() -> i32 {
    let arr = [10, 20, 12]
    let first = arr[0]
    let second = arr[1]
    let third = arr[2]
    return sum3(first, second, third)
}
