struct Point {
    x: i32
    y: i32
}

fn make_point(x: i32, y: i32) -> Point {
    return Point { x: x, y: y }
}

fn sum_point(p: Point) -> i32 {
    return p.x + p.y
}

fn main() -> i32 {
    let p = make_point(20, 22)
    return sum_point(p)
}
